# Normalize line endings: LF in the repo and in every working tree.
# Prevents CRLF from creeping in on Windows checkouts (core.autocrlf).
* text=auto eol=lf

# Windows-native scripts were committed with CRLF; leave them untouched.
*.bat -text
*.cmd -text
*.ps1 -text
